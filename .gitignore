bin/
obj/
node_modules/
dist/
.vs/
.vscode/
.msp.json
*.user

# docs/, tasks/ and CLAUDE.md are tracked in this repo — they carry the plan, the task queue and the
# always-loaded project instructions. Do not add them back here; a fresh clone would silently never
# commit them (see docs/FOUNDRY-PLAN.md, Step 1).

# secrets — never commit
appsettings.Development.json
appsettings.Local.json
.env
.env.*
!.env.example
secrets/

# claude code personal scope
CLAUDE.local.md
.claude/settings.local.json

# test + tooling output
TestResults/
coverage/
*.trx
evals-report.json
